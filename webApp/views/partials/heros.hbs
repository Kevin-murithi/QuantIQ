<section class="background-gradient py-16">
    <div class="container mx-auto text-center">
        <h1 class="text-4xl font-bold mb-4">
            Unlock the Power of Data with <span class="text-blue-500">{{brandName}}</span>
        </h1>
        <p class="text-lg mb-8">
            {{description}}
        </p>
        <div class="flex justify-center">
            <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mr-4">{{buttonText}}</button>
            <a href="{{learnMoreLink}}" class="text-blue-500 hover:underline">{{learnMoreText}}</a>
        </div>
    </div>
</section>
